{
  "name": "Execore Frontier C++2026 Hermetic Environment",
  "image": "mcr.microsoft.com/devcontainers/cpp:debian-12",
  "customizations": {
    "vscode": {
      "extensions": [
        "llvm-vs-code-extensions.vscode-clangd",
        "ms-vscode.cmake-tools",
        "twxs.cmake",
        "matepek.vscode-catch2-test-adapter"
      ],
      "settings": {
        "cmake.configureOnOpen": true,
        "cmake.defaultBuildPreset": "dev",
        "clangd.arguments": [
          "--compile-commands-dir=build/dev-clang",
          "--background-index",
          "--clang-tidy",
          "--completion-style=detailed",
          "--header-insertion=iwyu"
        ]
      }
    }
  },
  "postCreateCommand": "apt-get update && apt-get install -y wget lsb-release gnupg software-properties-common ninja-build cmake git python3 python3-pip mold && pip3 install --break-system-packages --upgrade cmake && (wget -qO- https://apt.llvm.org/llvm.sh | bash -s -- 19) || apt-get install -y clang clang-tidy clang-format lld"
}
